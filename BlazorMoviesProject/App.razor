@using BlazorMoviesProject.Pages.Movies
@* <Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
 *@


@* 
 <Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
            <Authorizing>
                <p>Por favor, espere mientras autorizamos el usuario</p>
            </Authorizing>
            <NotAuthorized>
                <BlazorMoviesProject.Pages.RedireccionarAlAcceso></BlazorMoviesProject.Pages.RedireccionarAlAcceso>
            </NotAuthorized>
        </AuthorizeRouteView>
    </Found>
    <NotFound>
        <CascadingAuthenticationState>
            <LayoutView Layout="@typeof(MainLayout)">
                <p>Error, no se encuentra nada en esta dirección</p>
            </LayoutView>
        </CascadingAuthenticationState>
    </NotFound>
</Router> *@



@*             <CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <Authorizing>
                    <p>Por favor, espere mientras autorizamos al usuario</p>
                </Authorizing>
                <NotAuthorized>
                    <BlazorMoviesProject.Pages.RedireccionarAlAcceso></BlazorMoviesProject.Pages.RedireccionarAlAcceso>
                </NotAuthorized>
            </AuthorizeRouteView>
        </Found>
        <NotFound>
            <LayoutView Layout="@typeof(MainLayout)">
                <p>Error, no se encuentra nada en esta dirección</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState> *@



<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
            <Authorizing>
                <p>Por favor, espere mientras autorizamos el usuario</p>
            </Authorizing>
            <NotAuthorized>
                <BlazorMoviesProject.Pages.RedireccionarAlAcceso></BlazorMoviesProject.Pages.RedireccionarAlAcceso>
            </NotAuthorized>
        </AuthorizeRouteView>
    </Found>
    <NotFound>
        <CascadingAuthenticationState>
            <LayoutView Layout="@typeof(MainLayout)">
                <p>Error, no se encuentra nada en esta dirección</p>
            </LayoutView>
        </CascadingAuthenticationState>
    </NotFound>
</Router>
